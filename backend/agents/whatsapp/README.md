# WhatsApp Agent (placeholder)

Future sub-backend for WhatsApp conversations. Same pattern as `../voice/`:

- Python worker in this folder
- Main backend routes sessions via `WHATSAPP_BACKEND_URL`
- UI lives in `flow-ai/`
